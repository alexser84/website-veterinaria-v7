---
import siteConfig from '../data/siteConfig.json';

const { negocio } = siteConfig;
---

<section class="relative min-h-[80vh] flex items-center overflow-hidden py-20 bg-[var(--background)]">
  <!-- Background Accents -->
  <div class="absolute top-0 right-0 w-[50%] h-full bg-[var(--light)] -skew-x-12 translate-x-1/4 -z-10"></div>
  <div class="absolute -bottom-20 -left-20 w-80 h-80 bg-[var(--primary)]/5 rounded-full blur-3xl -z-10"></div>
  
  <div class="container">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <!-- Content -->
      <div class="animate-fade-in-up">
        <span class="inline-block py-1 px-4 rounded-full bg-[var(--primary)]/10 text-[var(--primary)] font-bold text-sm mb-6 uppercase tracking-wider">
          {negocio.nombre}
        </span>
        
        <h1 class="text-5xl lg:text-7xl font-black text-[var(--dark)] leading-tight mb-8">
          Excelente <span class="text-[var(--primary)]">Salud Animal</span> y Nutrición Premium
        </h1>
        
        <p class="text-xl text-[var(--text-secondary)] mb-10 leading-relaxed max-w-xl">
          Combinamos servicios veterinarios de vanguardia con los mejores productos para el bienestar integral de tu mascota. Más que una clínica, somos su segundo hogar.
        </p>
        
        <!-- Trust Indicators -->
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-12">
          <div class="flex items-center gap-3">
            <div class="w-12 h-12 rounded-xl bg-white shadow-lg flex items-center justify-center text-[var(--primary)]">
              <i data-lucide="award"></i>
            </div>
            <div>
              <p class="font-bold text-lg leading-none">{negocio.años_experiencia}+</p>
              <p class="text-xs opacity-60">Años de Exp.</p>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="w-12 h-12 rounded-xl bg-white shadow-lg flex items-center justify-center text-[var(--primary)]">
              <i data-lucide="shield-check"></i>
            </div>
            <div>
              <p class="font-bold text-lg leading-none">Certificado</p>
              <p class="text-xs opacity-60">SAG e ISO</p>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="w-12 h-12 rounded-xl bg-white shadow-lg flex items-center justify-center text-[var(--primary)]">
              <i data-lucide="users"></i>
            </div>
            <div>
              <p class="font-bold text-lg leading-none">10k+</p>
              <p class="text-xs opacity-60">Clientes Felices</p>
            </div>
          </div>
        </div>
        
        <div class="flex flex-wrap gap-4">
          <a href="#tienda" class="btn-primary flex items-center gap-2 text-lg px-8">
            <i data-lucide="shopping-bag" class="w-5 h-5"></i>
            Explorar Tienda
          </a>
          <a href="#agendar" class="btn-secondary flex items-center gap-2 text-lg px-8">
            <i data-lucide="calendar" class="w-5 h-5"></i>
            Agendar Consulta
          </a>
        </div>
        
        <div class="mt-12 flex items-center gap-8 opacity-40 grayscale">
          <span class="text-xs font-bold uppercase tracking-widest">Nuestras Certificaciones:</span>
          <div class="flex gap-6 items-center">
            <span class="font-black text-xl italic">SAG</span>
            <span class="font-black text-xl italic">ISO 9001</span>
            <span class="font-black text-xl italic">CMVCH</span>
          </div>
        </div>
      </div>
      
      <!-- Image Side -->
      <div class="relative lg:block hidden">
        <div class="relative rounded-3xl overflow-hidden shadow-2xl border-8 border-white group">
          <img 
            src="/images/hero-vet.webp" 
            alt="Veterinario profesional" 
            class="w-full h-[600px] object-cover transition-transform duration-700 group-hover:scale-105"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-black/40 to-transparent"></div>
          
          <!-- Floating cards -->
          <div class="absolute top-10 right-10 bg-white p-4 rounded-2xl shadow-xl animate-bounce-slow">
            <div class="flex items-center gap-3">
              <div class="bg-yellow-400 p-2 rounded-lg text-white">
                <i data-lucide="star" class="w-4 h-4 fill-current"></i>
              </div>
              <div>
                <p class="font-black text-xl leading-none">4.9</p>
                <p class="text-[10px] uppercase font-bold opacity-60">Rating Google</p>
              </div>
            </div>
          </div>
          
          <div class="absolute bottom-10 left-10 bg-white p-4 rounded-2xl shadow-xl">
            <div class="flex items-center gap-3">
              <div class="bg-[var(--secondary)] p-2 rounded-lg text-white">
                <i data-lucide="heart" class="w-4 h-4 fill-current"></i>
              </div>
              <div>
                <p class="font-black text-xl leading-none">98%</p>
                <p class="text-[10px] uppercase font-bold opacity-60">Satisfacción</p>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Decoration -->
        <div class="absolute -top-10 -right-10 w-40 h-40 bg-[var(--accent)]/10 rounded-full blur-2xl -z-10"></div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes bounce-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  .animate-bounce-slow {
    animation: bounce-slow 4s infinite ease-in-out;
  }
</style>
